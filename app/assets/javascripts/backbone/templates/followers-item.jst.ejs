<div class="panel panel-default user-box">
    <div class="panel-body">
        <img src="<%= model.get('avatar').thumb %>" alt="<%= model.get('username') %>" class="user-img img-square pull-left" height="100"/>
        <div class="user-info text-center pull-left">
            <a href="/users/<%= model.get('username') %>" class="username"><%= model.get('full_name') %></a>
            <a href="#" class="user-follower">40 followers</a>
        </div>
        <% if (model.get('follows')) { %>
            <a href="javascript:void(0)" class="btn btn-info btn-following js-unfollow">Following</a>
        <% } else { %>
            <a href="javascript:void(0)" class="btn btn-default btn-follow js-follow">Follow</a>
        <% } %>
    </div>
</div>
