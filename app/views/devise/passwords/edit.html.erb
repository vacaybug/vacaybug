    <% title 'Change Password' %>
<div class="container" id="devise-wrapper">
    <div class="row">
        <div class="col-md-8 col-md-offset-2">
            <h2 class="login-text">Change Your Password</h2>
            <p class="login-subtext">Adventure Starts Here</p>
        </div>
    </div>
    <div class="row">
        <div class="col-md-4 col-md-offset-4">
            <%= devise_error_messages! %>
            <h4 class="text-center">Enter Your New Password</h4>
            <%= form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :put }) do |f| %>
            	<%= f.hidden_field :reset_password_token %>
                <div class="form-group">
                    <div class="input-group">
                        <span class="input-group-addon"><i class="fa fa-lock"></i></span>
                        <%= f.password_field :password, :class => 'form-control', :placeholder => 'New Password' %>
                    </div>
                </div>
                <div class="form-group">
                    <div class="input-group">
                        <span class="input-group-addon"><i class="fa fa-lock"></i></span>
                        <%= f.password_field :password_confirmation, :class => 'form-control', :placeholder => 'Confirm New Password' %>
                    </div>
                </div>
                <div class="row">
                    <div class="col-sm-6 col-sm-offset-3">
                        <div class="form-group">
                            <div><%= f.submit "Change My Password", :class => 'btn btn-primary btn-block' %></div>
                        </div>
                    </div>
                </div>
            <% end %>
        </div>
    </div>
</div>
